{
  "name": "@phantom-labs/engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "test": "python -m pytest tests/",
    "lint": "python -m ruff check ."
  }
}
